import React from "react";
import ReactDOM from "react-dom";
import AppRegister from "../Form/AppRegister";

ReactDOM.render(
  <React.StrictMode>
    <h1>hi this is akash!</h1>
    <AppRegister />
  </React.StrictMode>,
  document.getElementById("root")
);
